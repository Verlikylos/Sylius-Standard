<h1 id="sylius-product-name" class="ui monster dividing header" {{ sylius_test_html_attribute('product-name', product.name) }}>{{ product.name }}</h1>
{% if product.color %}
    <h5>
        {{ ('app.form.product.color' | trans) ~ ':' }}
        {{ product.color }}
    </h5>
{% endif %}
